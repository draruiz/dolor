---

---

<section class='pricing' id='paquetes'>
  <div class='pricing-bg'></div>

  <div class='container'>
    <div class='pricing-header fade-in'>
      <span class='section-subtitle'>Tu Inversión</span>
      <h2 class='section-title'>Modalidades de Acompañamiento</h2>
      <div class='divider-center'>✦</div>
      <p class='section-desc'>
        Elegir sanar es el acto de amor propio más valioso.
      </p>
    </div>

    <div class='pricing-grid'>
      <!-- Opción Holística -->
      <div class='pricing-card card-holistic fade-in-up'>
        <div class='card-glow'></div>
        <div class='ornament-top'></div>

        <div class='card-header'>
          <h3 class='card-title'>Enfoque Holístico</h3>
          <h4 class='card-guide'>Guiado por Willy</h4>
          <p class='card-desc'>
            Acompañamiento desde la tanatología y la dimensión espiritual para
            transitar el duelo con paz.
          </p>
        </div>

        <div class='pricing-body'>
          <div class='price-row'>
            <span class='price-type'>Por sesión</span>
            <div class='price-tag'>
              <span class='currency'>USD</span>
              <span class='amount'>65</span>
            </div>
            <span class='price-local'>COP $190.000 aprox.</span>
          </div>

          <div class='price-separator'>
            <span>✨ Paquete recomendado ✨</span>
          </div>

          <div class='price-row highlight'>
            <span class='price-type'>Paquete 8 Sesiones</span>
            <div class='price-tag'>
              <span class='currency'>USD</span>
              <span class='amount'>440</span>
            </div>
            <span class='price-local'>($55 por sesión - Ahorras $80)</span>
            <span class='price-local-total'>COP $1.672.000 aprox.</span>
          </div>

          <div class='features-list'>
            <ul>
              <li>
                <span class='gold-bullet'>✦</span> 8 sesiones (45 min cada una)
              </li>
              <li><span class='gold-bullet'>✦</span> Soporte vía WhatsApp</li>
              <li>
                <span class='gold-bullet'>✦</span> Guía de meditación personalizada
              </li>
              <li><span class='gold-bullet'>✦</span> Ritual de cierre</li>
            </ul>
          </div>
        </div>

        <div class='card-footer'>
          <a
            href='https://wa.me/573001234567?text=Hola,%20me%20interesa%20la%20opción%20Holística'
            target='_blank'
            class='cta-button primary'
          >
            Reservar Espacio
          </a>
        </div>
      </div>

      <!-- Opción Integral (Clinical + Holistic) -->
      <div
        class='pricing-card card-integral fade-in-up'
        style='animation-delay: 0.2s;'
      >
        <div class='card-glow'></div>
        <span class='badge-featured'>Más Completo</span>

        <div class='card-header'>
          <h3 class='card-title'>Enfoque Integral</h3>
          <h4 class='card-guide'>Equipo Clínico + Willy</h4>
          <p class='card-desc'>
            La unión profunda de la psicología clínica y la terapia espiritual
            para una sanación total.
          </p>
        </div>

        <div class='pricing-body'>
          <div class='price-row'>
            <span class='price-type'>Por sesión</span>
            <div class='price-tag'>
              <span class='currency'>USD</span>
              <span class='amount'>65</span>
            </div>
            <span class='price-local'>COP $190.000 aprox.</span>
          </div>

          <div class='price-separator'>
            <span>✨ Paquete recomendado ✨</span>
          </div>

          <div class='price-row highlight'>
            <span class='price-type'>Paquete 8 Sesiones</span>
            <div class='price-tag'>
              <span class='currency'>USD</span>
              <span class='amount'>440</span>
            </div>
            <span class='price-local'>($55 por sesión - Ahorras $80)</span>
            <span class='price-local-total'>COP $1.672.000 aprox.</span>
          </div>

          <div class='features-list'>
            <ul>
              <li>
                <span class='gold-bullet'>✦</span> 4 Sesiones Clínicas (Equipo Clínico)
              </li>
              <li>
                <span class='gold-bullet'>✦</span> 4 Sesiones Holísticas (Willy)
              </li>
              <li>
                <span class='gold-bullet'>✦</span> Abordaje de trauma y duelo
              </li>
              <li>
                <span class='gold-bullet'>✦</span> Herramientas clínicas y energéticas
              </li>
            </ul>
          </div>
        </div>

        <div class='card-footer'>
          <a
            href='https://wa.me/573001234567?text=Hola,%20me%20interesa%20la%20opción%20Integral'
            target='_blank'
            class='cta-button gold-filled'
          >
            Reservar Espacio
          </a>
        </div>
      </div>
    </div>

    <div class='payment-methods fade-in'>
      <p>
        Aceptamos tarjetas de crédito, PayPal y transferencias bancarias
        (Colombia).
      </p>
    </div>
  </div>
</section>

<style>
  .pricing {
    position: relative;
    padding: var(--section-padding) 0;
    background: #fff;
    overflow: hidden;
  }

  .pricing-bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(212, 175, 55, 0.03) 0%,
      rgba(255, 255, 255, 0) 50%,
      rgba(212, 175, 55, 0.03) 100%
    );
    pointer-events: none;
  }

  .pricing-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .divider-center {
    font-size: 1.5rem;
    color: var(--gold);
    text-align: center;
    margin: 1rem 0 2rem;
  }

  .section-desc {
    color: var(--gray-text);
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
    max-width: 900px;
    margin: 0 auto;
  }

  /* Card Styles */
  .pricing-card {
    position: relative;
    background: #fff;
    border: 1px solid var(--gold-light);
    border-radius: 4px; /* Slightly rounded, elegant */
    padding: 3rem 2rem;
    display: flex;
    flex-direction: column;
    transition: all 0.4s ease;
  }

  .pricing-card:hover {
    box-shadow: 0 20px 50px rgba(212, 175, 55, 0.15);
    transform: translateY(-5px);
    border-color: var(--gold);
  }

  /* Specific Card Styling */
  .card-holistic {
    border-top: 4px solid var(--gold-light);
  }

  .card-integral {
    border: 1px solid var(--gold);
    background: linear-gradient(160deg, #fffcf7 0%, #ffffff 100%);
    box-shadow: 0 10px 30px rgba(212, 175, 55, 0.1);
  }

  .badge-featured {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--gold);
    color: #fff;
    padding: 0.25rem 1rem;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 20px;
    box-shadow: 0 4px 10px rgba(212, 175, 55, 0.3);
  }

  /* Card Content */
  .card-header {
    text-align: center;
    margin-bottom: 2rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid rgba(212, 175, 55, 0.2);
  }

  .card-title {
    font-family: var(--font-serif);
    font-size: 2rem;
    color: var(--gold-dark);
    margin: 0 0 0.5rem;
  }

  .card-guide {
    font-family: var(--font-sans);
    font-size: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    margin: 0 0 1rem;
  }

  .card-desc {
    font-size: 1.1rem;
    color: var(--gray-text);
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .card-title {
      font-size: 1.8rem;
    }

    .card-guide {
      font-size: 0.95rem;
    }

    .card-desc {
      font-size: 1.05rem;
    }
  }

  /* Pricing Body */
  .pricing-body {
    flex-grow: 1;
  }

  .price-row {
    text-align: center;
    margin-bottom: 1.5rem;
  }

  .price-type {
    display: block;
    font-size: 1rem;
    text-transform: uppercase;
    color: var(--gray-medium);
    margin-bottom: 0.25rem;
  }

  .price-tag {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    color: var(--gray-dark);
    line-height: 1;
  }

  .currency {
    font-size: 1.1rem;
    margin-top: 5px;
    margin-right: 2px;
  }

  .amount {
    font-family: var(--font-serif);
    font-size: 2.8rem;
    font-weight: 500;
  }

  .price-local {
    display: block;
    font-size: 1rem;
    color: var(--gray-medium);
    margin-top: 0.5rem;
  }

  .price-local-total {
    display: block;
    font-size: 1rem;
    color: var(--gray-text);
    font-weight: 500;
  }

  @media (max-width: 768px) {
    .price-type {
      font-size: 0.95rem;
    }

    .currency {
      font-size: 1.05rem;
    }

    .amount {
      font-size: 2.5rem;
    }

    .price-local,
    .price-local-total {
      font-size: 0.95rem;
    }
  }

  .price-separator {
    text-align: center;
    margin: 1.5rem 0;
    position: relative;
  }

  .price-separator span {
    background: transparent;
    padding: 0 1rem;
    font-size: 0.8rem;
    color: var(--gold);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .highlight .amount {
    color: var(--gold-dark);
  }

  /* Features List */
  .features-list {
    margin-top: 2rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.5);
    border-radius: 4px;
  }

  .features-list ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .features-list li {
    display: flex;
    align-items: center;
    font-size: 1.1rem;
    color: var(--gray-text);
    margin-bottom: 0.8rem;
  }

  @media (max-width: 768px) {
    .features-list li {
      font-size: 1.05rem;
    }
  }

  .gold-bullet {
    color: var(--gold);
    margin-right: 0.8rem;
    font-size: 1.2rem;
  }

  .card-footer {
    margin-top: auto;
    padding-top: 2rem;
    text-align: center;
  }

  /* Buttons */
  .cta-button {
    display: inline-block;
    padding: 0.8rem 2rem;
    font-family: var(--font-sans);
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-decoration: none;
    border: 1px solid var(--gold);
    color: var(--gold-dark);
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: var(--gold-light);
    color: #fff;
    border-color: var(--gold-light);
  }

  .cta-button.gold-filled {
    background: var(--gold);
    color: #fff;
    border-color: var(--gold);
  }

  .cta-button.gold-filled:hover {
    background: var(--gold-dark);
    border-color: var(--gold-dark);
  }

  .payment-methods {
    text-align: center;
    margin-top: 4rem;
    font-size: 1.05rem;
    color: var(--gray-medium);
  }

  @media (max-width: 768px) {
    .payment-methods {
      font-size: 1rem;
    }
  }

  @media (max-width: 850px) {
    .pricing-grid {
      grid-template-columns: 1fr;
    }

    .pricing-card {
      margin-bottom: 2rem;
    }
  }
</style>
