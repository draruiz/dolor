---

---

<section class='about' id='equipo'>
  <div class='about-bg'></div>

  <div class='container'>
    <div class='about-header fade-in'>
      <span class='section-subtitle'>Tu Acompañamiento</span>
      <h2 class='section-title'>¿Quién te acompaña en este proceso?</h2>
      <div class='divider-center'>✦</div>
    </div>

    <div class='team-grid'>
      <!-- Willy Card -->
      <div class='team-card fade-in-left'>
        <div class='card-image-wrapper'>
          <div class='gold-halo'></div>
          <div class='image-frame-oval'>
            <div class='image-placeholder'>
              <span class='placeholder-icon'>W</span>
            </div>
          </div>
          <!-- SVG Border Decoration -->
          <svg class='portal-frame' viewBox='0 0 160 220'>
            <ellipse
              cx='80'
              cy='110'
              rx='75'
              ry='105'
              fill='none'
              stroke='url(#gold-gradient-about)'
              stroke-width='1.5'></ellipse>
            <circle cx='80' cy='5' r='3' fill='#d4af37'></circle>
            <circle cx='80' cy='215' r='3' fill='#d4af37'></circle>
            <circle cx='5' cy='110' r='3' fill='#d4af37'></circle>
            <circle cx='155' cy='110' r='3' fill='#d4af37'></circle>
            <defs>
              <linearGradient
                id='gold-gradient-about'
                x1='0%'
                y1='0%'
                x2='100%'
                y2='100%'
              >
                <stop offset='0%' stop-color='#d4af37'></stop>
                <stop offset='50%' stop-color='#f3e5ab'></stop>
                <stop offset='100%' stop-color='#b8962e'></stop>
              </linearGradient>
            </defs>
          </svg>
        </div>

        <div class='card-content'>
          <div class='card-header'>
            <h3 class='team-name'>Willy</h3>
            <p class='team-role'>Tanatólogo y Guía Espiritual</p>
          </div>

          <div class='card-badges'>
            <span class='badge'>Tanatólogo</span>
            <span class='badge'>Maestro Reiki</span>
            <span class='badge'>Angeólogo</span>
          </div>

          <div class='card-text'>
            <p>
              Desde muy pequeño posee la capacidad de percibir otras dimensiones
              de la existencia. Ha dedicado su vida a acompañar procesos de
              duelo y transición del alma.
            </p>
          </div>

          <div class='card-experience'>
            <ul>
              <li>Procesos de duelo por fallecimiento</li>
              <li>Acompañamiento en transiciones del alma</li>
              <li>Búsqueda de familiares desaparecidos</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- Dra. Ruiz Card -->
      <div class='team-card fade-in-right'>
        <div class='card-image-wrapper'>
          <div class='gold-halo'></div>
          <div class='image-frame-oval'>
            <div class='image-placeholder'>
              <span class='placeholder-icon'>MR</span>
            </div>
          </div>
          <!-- SVG Border Decoration -->
          <svg class='portal-frame' viewBox='0 0 160 220'>
            <ellipse
              cx='80'
              cy='110'
              rx='75'
              ry='105'
              fill='none'
              stroke='url(#gold-gradient-about)'
              stroke-width='1.5'></ellipse>
            <circle cx='80' cy='5' r='3' fill='#d4af37'></circle>
            <circle cx='80' cy='215' r='3' fill='#d4af37'></circle>
            <circle cx='5' cy='110' r='3' fill='#d4af37'></circle>
            <circle cx='155' cy='110' r='3' fill='#d4af37'></circle>
          </svg>
        </div>

        <div class='card-content'>
          <div class='card-header'>
            <h3 class='team-name'>Dra. María Alejandra Ruiz</h3>
            <p class='team-role'>Psicóloga Clínica</p>
          </div>

          <div class='card-badges'>
            <span class='badge'>Psicóloga Clínica</span>
            <span class='badge'>Terapia Integradora</span>
          </div>

          <div class='card-text'>
            <p>
              Integra la psicología clínica con una visión holística profunda.
              Juntos acompañan a quienes han perdido a seres queridos o
              atraviesan duelos traumáticos.
            </p>
          </div>

          <div class='card-highlight'>
            <div class='highlight-icon'>❋</div>
            <p>
              Un enfoque que une <strong>ciencia y espiritualidad</strong> para sanar
              el corazón.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Joint Work Banner -->
    <div class='joint-work fade-in'>
      <div class='joint-border'></div>
      <h3>Trabajo Conjunto</h3>
      <p>
        <strong>Psicología Clínica + Terapia Holística</strong><br />
        Para acompañarte de manera integral en tu proceso.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    position: relative;
    padding: var(--section-padding) 0;
    background: linear-gradient(
      180deg,
      var(--white-cream) 0%,
      var(--white-pure) 100%
    );
    overflow: hidden;
  }

  .divider-center {
    font-size: 1.5rem;
    color: var(--gold);
    text-align: center;
    margin: 1rem 0 3rem;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  .divider-center::before,
  .divider-center::after {
    content: "";
    height: 1px;
    width: 60px;
    background: linear-gradient(90deg, transparent, var(--gold));
  }

  .divider-center::after {
    background: linear-gradient(90deg, var(--gold), transparent);
  }

  .about-bg {
    position: absolute;
    inset: 0;
    opacity: 0.3;
    pointer-events: none;
    background-image: radial-gradient(#d4af37 1px, transparent 1px);
    background-size: 40px 40px;
  }

  .about-header {
    text-align: center;
    margin-bottom: 5rem;
  }

  /* Team Grid */
  .team-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 4rem;
    max-width: 1000px;
    margin: 0 auto 5rem;
  }

  .team-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  /* Card Image */
  .card-image-wrapper {
    position: relative;
    width: 200px;
    height: 280px; /* Oval aspect ratio */
    margin-bottom: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .gold-halo {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 180px;
    height: 240px;
    background: radial-gradient(
      ellipse at center,
      rgba(212, 175, 55, 0.2) 0%,
      transparent 70%
    );
    z-index: 0;
  }

  .image-frame-oval {
    width: 150px;
    height: 210px;
    border-radius: 50%; /* Make it oval-ish by dimensions + radius */
    border-radius: 75px / 105px;
    overflow: hidden;
    position: relative;
    z-index: 1;
    background: var(--beige-soft);
  }

  .image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f0ece6;
  }

  .placeholder-icon {
    font-family: var(--font-serif);
    font-size: 2rem;
    color: var(--gold);
    opacity: 0.5;
  }

  .portal-frame {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    z-index: 2;
    pointer-events: none;
  }

  /* Card Content */
  .card-content {
    max-width: 400px;
  }

  .card-header {
    margin-bottom: 1.5rem;
  }

  .team-name {
    font-family: var(--font-serif);
    font-size: 1.8rem;
    font-weight: 300;
    color: var(--gray-dark);
    margin: 0 0 0.5rem;
  }

  .team-role {
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    margin: 0;
  }

  /* Badges */
  .card-badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.8rem;
    margin-bottom: 1.5rem;
  }

  .badge {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 0.95rem;
    color: var(--gray-text);
    position: relative;
    padding: 0 0.5rem;
  }

  .badge:not(:last-child):after {
    content: "•";
    position: absolute;
    right: -0.5rem;
    color: var(--gold-light);
  }

  /* Card Text */
  .card-text p {
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--gray-text);
  }

  /* Experience List */
  .card-experience ul {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: 0.85rem;
    color: var(--gray-medium);
  }

  .card-experience li {
    margin-bottom: 0.4rem;
  }

  .card-highlight {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--gold-light);
  }

  .highlight-icon {
    color: var(--gold);
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }

  /* Joint Work Banner */
  .joint-work {
    text-align: center;
    padding: 3rem;
    max-width: 700px;
    margin: 0 auto;
    position: relative;
    background: #fff;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.03);
  }

  .joint-border {
    position: absolute;
    inset: 6px;
    border: 1px solid var(--gold-light);
    opacity: 0.5;
    pointer-events: none;
  }

  .joint-work h3 {
    font-family: var(--font-serif);
    font-size: 1.4rem;
    font-weight: 400;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold-dark);
    margin-bottom: 1rem;
  }

  /* Responsive */
  @media (max-width: 968px) {
    .team-grid {
      grid-template-columns: 1fr;
      gap: 4rem;
    }
  }
</style>
